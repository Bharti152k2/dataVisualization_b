{
  "version": 2,
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/index.js"
    }
  ],
  "includeFiles": [
    "controller/**/*.js",
    "database/**/*.js",
    "helpers/**/*.js",
    "models/**/*.js",
    "routes/**/*.js"
  ]
}
